import setuptools

TEMPLATE_NAME = "storage_bigquery"
VERSION = "0.0.1"
DESCRIPTION = ""

DEPENDENCIES = [
    
]

setuptools.setup(
    name=TEMPLATE_NAME,
    version=VERSION,
    DESCRIPTION=DESCRIPTION,
    author="me",
    packages=setuptools.find_packages()
)